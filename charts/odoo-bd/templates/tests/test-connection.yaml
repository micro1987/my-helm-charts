apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odoo-bd.fullname" . }}-test-connection"
  labels:
    {{- include "odoo-bd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odoo-bd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
